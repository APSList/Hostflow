apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "communication-service.fullname" . }}-test-connection"
  labels:
    {{- include "communication-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "communication-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
