apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "communications.fullname" . }}-test-connection"
  labels:
    {{- include "communications.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "communications.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
